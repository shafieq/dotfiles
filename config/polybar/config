[colors]
darkred = 	${xrdb:color1:#cc241d}
darkgreen = 	${xrdb:color2:#98971a}
darkyellow = 	${xrdb:color3:#d79921}
darkblue = 	${xrdb:color4:#458588}
darkmagenta = 	${xrdb:color5:#b16286}
darkcyan = 	${xrdb:color6:#689d6a}
lightgray = 	${xrdb:color7:#a89984}
gray = 		${xrdb:color8:#928374}
red = 		${xrdb:color9:#fb4934}
green = 	${xrdb:color10:#b8bb26}
yellow = 	${xrdb:color11:#fabd2f}
blue = 		${xrdb:color12:#83a598}
magenta = 	${xrdb:color13:#d3869b}
cyan = 		${xrdb:color14:#8ec07c}
background = 	${xrdb:color0:#282828}
foreground = 	${xrdb:color15:#edbbb2}

[bar/default]
;monitor = ${env:MONITOR:DisplayPort-0}
;monitor = ${env:MONITOR:LVDS-1}
;monitor = ${env:MONITOR:HDMI-1}
enable-ipc = true
width = 100%
height = 22
separator =

background = #121212
foreground = #bcbcbc

line-size = 1
line-color = {$xrdb:red}

border-size = 0
border-color = ${colors.foreground}

padding-left = 0
padding-right = 1

module-margin-left = 1
module-margin-right = 1

font-0 = Hack Nerd Font:pixelsize=10;1
font-1 = NotoColorEmoji:fontformat=truetype:scale=12:antialias=false;
font-2 = FontAwesome5Free:fontformat=truetype:scale=5:antialias=false;
;font-2 = "Font Awesome 5 Free:style=Solid:pixelsize=15;3"

; Other modules: cpu uptime xbacklight weather filesystem temperature mutt-wizard memory
modules-left = bspwm xwindow
modules-center =
modules-right = cpu temperature weather volume wlan battery2 date

tray-position = right
tray-padding = 0

wm-restack = bspwm

;override-redirect = true

scroll-up = bspwm-desknext
scroll-down = bspwm-deskprev

[module/crypto]
type = custom/script
exec = crypto
interval = 0
click-left = $BROWSER https://coinmarketcap.com/ &
click-right = notifycrypto

[module/mutt-wizard]

type = custom/script
exec = mailbox
interval = 5
format-prefix = " "
;format-underline = ${colors.red}

click-left = st -e neomutt

[module/xwindow]
type = internal/xwindow
label = %title:0:50:...%
format-foreground = #bcbcbc

[module/filesystem]
type = internal/fs
interval = 25

mount-0 = /
mount-1 = /home

label-mounted-underline = ${colors.cyan}
label-mounted = /: %percentage_used%%
label-unmounted = %mountpoint% not mounted
label-unmounted-foreground = ${colors.gray}

[module/bspwm]
type = internal/bspwm

label-focused = %index%
label-focused-foreground = #dadada
label-focused-background = #458588
label-focused-underline= ${colors.foreground}
label-focused-padding = 1

label-occupied = %index%
label-occupied-background = #504945
label-occupied-underline = #dadada
label-occupied-padding = 1

label-urgent = %index%!
label-urgent-background = ${colors.red}
label-urgent-padding = 1

label-empty = %index%
label-empty-foreground = ${colors.lightgray}
label-empty-padding = 1

format = <label-state> <label-mode>

label-monocle = " (M)"
label-tiled = " (T) "
label-floating = " </> "
label-pseudotiled = " (t) "
label-locked = " 🔒 "
label-sticky = " 📌 "
label-private =  " ⛔ "

format-foreground = ${colors.foreground}
format-background = ${colors.background}

[module/xbacklight]
type = internal/xbacklight

; Use the following command to list available cards:
; $ ls -1 /sys/class/backlight/
card = intel_backlight
format =  <label>

; Available tokens:
;   %percentage% (default)
label = %percentage%%

[module/uptime]
;https://github.com/jaagr/polybar/wiki/User-contributed-modules#uptime
type = custom/script
exec = uptime | awk -F, '{sub(".*up ",x,$1);print $1}'
interval = 100
label =⏳ %output%

[module/cpu]
type = internal/cpu
interval = 3
format-prefix = " 💻 "
format-prefix-foreground = ${colors.darkblue}
;format-underline = ${colors.red}
label = %percentage%%

[module/wlan]
type = internal/network
interface = wlp2s0
interval = 5

label-connected = 📡 %essid%
label-connected-foreground = ${colors.red}
label-disconnected =

[module/weather]
type = custom/script
exec = weather
;label = "%output%"
click-left = $TERMINAL -e w3m wttr.in/Sungkai &
interval = 5000

[module/date]
type = internal/date
interval = 60

date = "(%a) %d %b %y"
time = "%H:%M"

format-foreground = $504945

format-prefix = "📅 "
format-prefix-foreground = ${colors.red}
format-underline = ${colors.background}

label = %date% 🕛 %time%

[module/volume]
type = custom/script
exec = volume
label = "%output%"
;click-left = $TERMINAL -e w3m wttr.in/Adelaide &
initial = 1

[module/battery]
type = internal/battery
battery = BAT0
adapter = ADP1
full-at = 99

format-charging = 🔌 <label-charging>
format-charging-underline = ${colors.background}

format-discharging = <ramp-capacity> <label-discharging>
format-discharging-underline = ${self.format-charging-underline}

format-full-prefix = "♻"
format-full-prefix-foreground = ${colors.green}
format-full-underline = ${self.format-charging-underline}

ramp-capacity-0 = 🔋

[module/battery2]
type = internal/battery
battery = BAT0
adapter = ADP1
full-at = 98

format-charging =  <label-charging>
format-charging-prefix-foreground = ${colors.yellow}
format-charging-underline = ${colors.background}

;format-discharging = <label-discharging>
format-discharging = <ramp-capacity> <label-discharging>
format-discharging-underline = ${self.format-charging-underline}

format-full-prefix = "  "
format-full-prefix-foreground = ${colors.green}
format-full-underline = ${self.format-charging-underline}

ramp-capacity-0 = 
ramp-capacity-1 = 
ramp-capacity-foreground = ${colors.green}

;animation-charging-0 = 
;animation-charging-1 = 
;animation-charging-2 = 
;animation-charging-3 = 
;animation-charging-4 = 
;animation-charging-foreground = ${colors.darkgreen}
;animation-charging-framerate = 500

[module/temperature]
type = internal/temperature
interval = 15

format = <ramp> <label>
label = %temperature-c%

ramp-0 = 🌡

;**********************************************************
;
;                       GLOBAL WM SETTINGS
;
;**********************************************************
[global/wm]
margin-top = 0
margin-bottom = 0
